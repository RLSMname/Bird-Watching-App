<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
    </domain-config>

    <!-- Optionally allow cleartext for all domains (for testing purposes) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">* </domain> <!-- Allow cleartext for all domains -->
    </domain-config>
</network-security-config>
