CREATE TABLE bird (
    id VARCHAR(36) PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    bird_order VARCHAR(255) NOT NULL,
    family VARCHAR(255) NOT NULL,
    habitat VARCHAR(255) NOT NULL,
    sight_count INT NOT NULL
);
